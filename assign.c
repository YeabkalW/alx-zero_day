#include <stdio.h>

/**
 * assignment operator copies R-value to L-value 
 * 
 * main - shows the assignment operator in action
 * 
 * return: always 0.
 * 
 */

int main (void)
{
    /**
     * home-work: what is the output of the following program segment? 
     */

    char a = 7;
    a ^= 5;
    printf("%d\n", printf("%d", a+=3));

    return 0;
}